# 本地开发环境设计方案

## 1. 目标

本方案面向当前仓库的日常研发，目标是同时满足：

- 需要开发前端代码
- 需要开发后端代码
- 方便 debug
- 方便效果展示、确认

## 2. 设计原则

本地开发环境采用以下原则：

1. 前后端源码直接跑在宿主机
2. 数据库、缓存、向量库、沙箱、插件守护进程等依赖跑在 Docker
3. 默认不把前后端业务代码放进开发容器里跑
4. 保留一套“演示入口”，方便产品、测试和开发一起确认效果

原因很直接：

- 前端源码在宿主机运行，Next.js 热更新和浏览器调试体验最好
- 后端源码在宿主机运行，Python 断点、日志、热重载最方便
- 中间件容器化，可以降低本机依赖安装复杂度
- 展示时可以统一走固定入口，减少“我这边能打开、你那边不行”的问题

## 3. 推荐架构

### 3.1 分层方式

建议把本地开发环境拆成三层：

| 层级 | 运行位置 | 内容 |
| --- | --- | --- |
| 前端开发层 | 宿主机 | `web` 源码，Next.js dev server |
| 后端开发层 | 宿主机 | `api` 源码，Flask + Celery |
| 基础设施层 | Docker | PostgreSQL、Redis、Weaviate、Sandbox、Plugin Daemon、SSRF Proxy |

### 3.2 运行拓扑

推荐拓扑如下：

```text
Browser
  -> http://localhost:3000        web dev server
  -> http://localhost:5001        api dev server

web (宿主机源码运行)
  -> 调用 api:5001

api (宿主机源码运行)
  -> PostgreSQL 容器
  -> Redis 容器
  -> Weaviate 容器
  -> Sandbox 容器
  -> Plugin Daemon 容器
  -> SSRF Proxy 容器

worker / beat (宿主机源码运行，可选)
  -> Redis / PostgreSQL / Weaviate
```

## 4. 为什么不用“全容器开发”

不建议把前端、后端源码都塞进容器里做主开发模式，原因如下：

- 前端 HMR、Source Map、浏览器调试、Node inspector 通常不如宿主机直跑顺手
- 后端 Python 断点、IDE Attach、单测执行、临时脚本调试在宿主机更直接
- 容器内文件同步、依赖缓存、权限问题会拖慢反馈速度
- 当前仓库已经提供了宿主机源码运行方式，没必要反向复杂化

容器化前后端可以保留为“验收/演示模式”，但不适合作为主开发模式。

## 5. 推荐的本地开发模式

## 5.1 模式 A：主开发模式

这是默认推荐模式。

### 容器里运行

- `db`
- `redis`
- `sandbox`
- `plugin_daemon`
- `ssrf_proxy`
- `weaviate`

直接复用仓库已有编排：

```bash
cd docker
cp middleware.env.example middleware.env
docker compose -f docker-compose.middleware.yaml --profile weaviate -p dify up -d
```

### 宿主机运行

- `api`
- `worker`
- `worker_beat`（按需）
- `web`

建议命令：

```bash
# 终端 1：后端 API
./dev/start-api

# 终端 2：异步任务 worker
./dev/start-worker

# 终端 3：前端
cd web
pnpm install
cp .env.example .env.local
pnpm dev
```

如果需要定时任务调试，再补一个终端：

```bash
cd api
uv run --project . celery -A app.celery beat --loglevel INFO
```

### 优点

- 前后端代码都支持热更新
- 最适合 IDE 断点调试
- 依赖容器固定，环境稳定
- 页面效果和接口行为可以快速联调

## 5.2 模式 B：演示确认模式

这是给产品、测试、设计或业务同学一起确认效果时用的模式。

建议保留两种方式：

### 方式 1：仍使用开发服务

- 前端继续跑 `pnpm dev`
- 后端继续跑 `flask run --debug`
- 直接访问 `http://localhost:3000`

适合内部快速看改动。

### 方式 2：切到接近生产的统一入口

演示前可启动完整 compose：

```bash
cd docker
cp .env.example .env
docker compose up -d
```

这套模式适合：

- 验证 Nginx 代理链路
- 验证容器化部署效果
- 给非研发同学演示
- 验证“开发完成后是否能按部署方式正常运行”

建议把它作为“确认模式”，而不是日常编码模式。

## 6. 端口规划

为了避免混乱，建议固定本地端口分配。

| 服务 | 端口 | 建议用途 |
| --- | --- | --- |
| 前端 Web | `3000` | 主访问入口 |
| 后端 API | `5001` | 接口与控制台 API |
| Plugin Daemon | `5002` | 插件服务 |
| Plugin Debugging | `5003` | 插件远程安装/调试 |
| PostgreSQL | `5432` | 数据库 |
| Redis | `6379` | 缓存和 Celery broker |
| Weaviate | `8080` | 默认向量库 |
| Sandbox | `8194` | 代码执行 |
| SSRF Proxy | `3128` | 沙箱代理 |

原则：

- 开发服务端口固定
- 文档、脚本、环境变量统一引用同一组端口
- 不要同时启动“宿主机 API”和“容器 API”占用同一个端口

## 7. 环境变量建议

## 7.1 前端

`web/.env.local` 建议至少明确以下变量：

```env
NEXT_PUBLIC_DEPLOY_ENV=DEVELOPMENT
NEXT_PUBLIC_EDITION=SELF_HOSTED
NEXT_PUBLIC_API_PREFIX=http://localhost:5001/console/api
NEXT_PUBLIC_PUBLIC_API_PREFIX=http://localhost:5001/api
```

这样前端直接连宿主机上的后端调试服务。

## 7.2 后端

`api/.env` 建议以 `api/.env.example` 为基础，并确保以下依赖指向 Docker 中间件：

- `DB_HOST`
- `DB_PORT`
- `REDIS_HOST`
- `REDIS_PORT`
- `WEAVIATE_ENDPOINT`
- `CODE_EXECUTION_ENDPOINT`
- `PLUGIN_DAEMON_URL`

如果后端源码在宿主机运行，而中间件在 Docker 中运行，需要注意地址：

- 数据库、Redis、Weaviate 暴露到宿主机后，通常填 `localhost`
- `plugin_daemon` 与 `sandbox` 也建议通过宿主机映射端口访问

因此宿主机开发时，建议优先使用类似：

```env
DB_HOST=127.0.0.1
REDIS_HOST=127.0.0.1
WEAVIATE_ENDPOINT=http://127.0.0.1:8080
CODE_EXECUTION_ENDPOINT=http://127.0.0.1:8194
PLUGIN_DAEMON_URL=http://127.0.0.1:5002
PLUGIN_DIFY_INNER_API_URL=http://host.docker.internal:5001
```

其中：

- `PLUGIN_DIFY_INNER_API_URL` 是给容器里的 `plugin_daemon` 回调宿主机 API 用的
- 这和 `docker-compose.middleware.yaml` 当前默认值一致

## 8. Debug 设计

## 8.1 前端 Debug

建议方式：

- WebStorm / VS Code 直接调试 Next.js
- 浏览器 DevTools 调试页面逻辑
- 使用 React DevTools
- 需要单独开发组件时，使用 Storybook

推荐命令：

```bash
cd web
pnpm dev
pnpm storybook
```

建议用途分工：

- `pnpm dev`：联调整体页面和接口
- `pnpm storybook`：开发独立组件、快速确认视觉效果

## 8.2 后端 Debug

建议方式：

- 宿主机直接运行 Flask debug server
- IDE 直接打断点
- 单独开 worker 终端调异步任务
- 必要时再开 beat 调度

当前仓库已经有现成脚本：

- `./dev/start-api`
- `./dev/start-worker`

优势：

- Python 栈信息完整
- 可以直接在 `controllers/services/tasks/core` 上打断点
- 任务队列问题能单独定位，不影响主 API 调试

## 8.3 插件与沙箱 Debug

这部分不建议一开始做深度容器内调试，先按“接口联通”设计：

- `plugin_daemon` 跑容器
- `sandbox` 跑容器
- 后端源码通过宿主机端口访问它们

只有在确实需要定位插件守护进程或沙箱内部问题时，再进入容器查看日志或二次定制。

## 9. 效果展示与确认设计

为了方便“开发完成后快速给别人看”，建议明确两个入口：

## 9.1 开发入口

- `http://localhost:3000`

用途：

- 开发自己查看
- 前后端联调
- UI 和交互快速确认

## 9.2 演示入口

建议增加一个统一的展示方式，二选一：

### 方案 1：固定本机入口

直接约定：

- 前端：`http://localhost:3000`
- 后端 Swagger / API：`http://localhost:5001`

适合开发团队内部。

### 方案 2：通过 Nginx 统一入口

使用完整 compose 后，通过：

- `http://localhost/install`
- `http://localhost`

作为统一入口。

适合：

- 产品验收
- 录屏演示
- 部署前冒烟确认

## 10. 推荐工作流

日常推荐工作流如下：

1. 启动中间件容器
1. 启动后端 `api`
1. 启动后端 `worker`
1. 启动前端 `web`
1. 在 `localhost:3000` 联调
1. 改动较大时，用完整 compose 再验证一次展示效果

## 11. 建议补充的工程化内容

如果你准备把这套方案长期固化，建议再补三类内容。

## 11.1 启动脚本

建议新增一组统一脚本，例如：

- `dev/start-middleware`
- `dev/start-web`
- `dev/start-beat`
- `dev/start-dev-stack`

目标是把当前分散命令统一成固定入口。

## 11.2 环境模板

建议补充：

- `api/.env.localdev.example`
- `web/.env.localdev.example`
- `docker/middleware.env.localdev.example`

这样可以减少“每个人自己猜环境变量”的成本。

## 11.3 演示脚本

建议增加一个“演示模式”脚本，例如：

- `scripts/demo_up.sh`
- `scripts/demo_down.sh`

用途：

- 一键切到完整展示环境
- 快速恢复到可演示状态

## 12. 最终推荐方案

最终推荐如下：

### 主开发模式

- 宿主机运行 `web`
- 宿主机运行 `api`
- 宿主机运行 `worker`
- Docker 运行 `db`、`redis`、`weaviate`、`sandbox`、`plugin_daemon`、`ssrf_proxy`

### 演示确认模式

- 使用完整 `docker/docker-compose.yaml`
- 通过统一入口进行最终确认

## 13. 一句话结论

最适合当前仓库的本地开发环境，不是“全部容器化开发”，而是“源码跑前后端、容器跑依赖、完整 compose 只用于展示和最终确认”的双模式设计。这套方案在调试效率、热更新体验和演示稳定性之间最平衡。
