# 工程模块与镜像、容器映射说明

## 1. 文档目的

本文梳理当前仓库中各个核心工程模块，与 Docker 镜像、Docker Compose 服务容器之间的对应关系，方便快速理解：

- 哪些模块有本仓库源码并可本地构建镜像
- 哪些服务是同一镜像通过不同启动模式复用
- 哪些容器属于基础设施、中间件或可选依赖
- 哪些镜像虽然出现在编排中，但源码并不在当前仓库内

本文主要依据以下文件整理：

- `docker/docker-compose.yaml`
- `docker/docker-compose.middleware.yaml`
- `docker/docker-compose-template.yaml`
- `api/Dockerfile`
- `api/docker/entrypoint.sh`
- `web/Dockerfile`
- `web/docker/entrypoint.sh`
- `docker/tidb/docker-compose.yaml`

## 2. 仓库内主要模块

当前仓库中，与运行时部署最直接相关的模块主要有：

| 模块目录 | 作用 | 是否有本仓库 Dockerfile |
| --- | --- | --- |
| `api/` | 后端 API、任务处理、调度相关核心实现 | 是 |
| `web/` | 前端 Web 控制台与应用界面 | 是 |
| `docker/` | Docker Compose 编排、Nginx/SSRF/PGVector/Couchbase 等容器辅助脚本 | 否，主要提供编排和脚本 |
| `docker/nginx/` | Nginx 模板和启动脚本 | 否，复用官方 `nginx` 镜像 |
| `docker/ssrf_proxy/` | Squid SSRF 代理模板和启动脚本 | 否，复用官方 `ubuntu/squid` 镜像 |
| `docker/pgvector/` | PGVector 容器启动脚本 | 否，复用官方 `pgvector/pgvector` 镜像 |
| `docker/couchbase-server/` | Couchbase 自定义镜像构建目录 | 是 |
| `docker/tidb/` | TiDB 独立编排示例 | 否，复用 PingCAP 官方镜像 |

## 3. 主结论

### 3.1 核心业务模块只对应两个源码镜像

当前仓库真正由源码直接构建的核心业务镜像，主要只有两个：

- `api/Dockerfile` -> `langgenius/dify-api:*`
- `web/Dockerfile` -> `langgenius/dify-web:*`

其中：

- `api` 镜像会被三个容器复用：`api`、`worker`、`worker_beat`
- `web` 镜像只对应一个容器：`web`

### 3.2 一个模块可映射多个容器

`api/` 模块并不是只对应一个服务，它通过环境变量 `MODE` 在同一镜像里分化出不同运行角色：

- `MODE=api` -> Gunicorn/Flask API 服务
- `MODE=worker` -> Celery Worker
- `MODE=beat` -> Celery Beat 定时调度

这也是当前工程里最关键的“一份代码、多类容器”的复用关系。

### 3.3 大多数中间件不是本仓库源码模块

数据库、缓存、向量库、代理、插件守护进程、沙箱、Nginx 等大多直接使用外部镜像或官方镜像，当前仓库主要负责：

- Compose 编排
- 环境变量注入
- 启动脚本挂载
- 数据卷挂载
- 少量自定义镜像构建

## 4. 核心模块 -> 镜像 -> 容器映射

### 4.1 `api/` 模块

| 项 | 说明 |
| --- | --- |
| 源码目录 | `api/` |
| Dockerfile | `api/Dockerfile` |
| 目标镜像 | `langgenius/dify-api:2.0.0-beta.2` |
| 入口脚本 | `api/docker/entrypoint.sh` |
| 在 Compose 中对应的容器 | `api`、`worker`、`worker_beat` |

#### 运行角色拆分

`api/docker/entrypoint.sh` 内部通过 `MODE` 决定启动方式：

| 容器服务名 | 镜像 | `MODE` | 实际进程 | 职责 |
| --- | --- | --- | --- | --- |
| `api` | `langgenius/dify-api:2.0.0-beta.2` | `api` | `gunicorn app:app` 或 `flask run` | 提供后端 HTTP API |
| `worker` | `langgenius/dify-api:2.0.0-beta.2` | `worker` | `celery ... worker` | 异步任务消费 |
| `worker_beat` | `langgenius/dify-api:2.0.0-beta.2` | `beat` | `celery ... beat` | 定时任务调度 |

#### 关键补充

- `api`、`worker` 共用 `./volumes/app/storage:/app/api/storage`
- `api`、`worker`、`worker_beat` 共用大量 `x-shared-env` 环境变量
- `api`、`worker` 都依赖 `db` 和 `redis`
- `api`、`worker`、`worker_beat` 都连接 `ssrf_proxy_network`
- 若 `MIGRATION_ENABLED=true`，镜像入口启动前会先执行 `flask upgrade-db`

#### 对源码的进一步对应关系

从目录职责看，`api/` 下面可以粗分为：

- `api/controllers/`：HTTP 控制器，对应 `api` 容器直接暴露的接口层
- `api/services/`、`api/core/`、`api/repositories/`：后端业务核心，既被 `api` 使用，也被 `worker`/`worker_beat` 复用
- `api/tasks/`、`api/schedule/`：更偏异步任务与调度，主要由 `worker`/`worker_beat` 消费
- `api/migrations/`：数据库迁移，启动时由同一镜像在入口脚本中触发

换句话说，`api/` 既是“后端接口模块”，也是“异步任务模块”和“调度模块”的代码承载体。

### 4.2 `web/` 模块

| 项 | 说明 |
| --- | --- |
| 源码目录 | `web/` |
| Dockerfile | `web/Dockerfile` |
| 目标镜像 | `langgenius/dify-web:2.0.0-beta.2` |
| 入口脚本 | `web/docker/entrypoint.sh` |
| 在 Compose 中对应的容器 | `web` |

#### 运行方式

`web/docker/entrypoint.sh` 会把后端地址和前端配置注入 `NEXT_PUBLIC_*` 环境变量，然后使用：

- `pm2 start /app/web/server.js --name dify-web -i ${PM2_INSTANCES}`

因此映射关系是：

- `web/` 源码 -> `langgenius/dify-web:*` 镜像 -> `web` 容器 -> Next.js 生产服务

#### 对源码的进一步对应关系

| 子目录 | 主要作用 |
| --- | --- |
| `web/app/` | Next.js App Router 页面与路由 |
| `web/service/` | 前端调用后端 API 的服务层 |
| `web/i18n/`、`web/i18n-config/` | 国际化资源与配置 |
| `web/context/`、`web/hooks/`、`web/models/` | 前端状态、交互逻辑和领域模型 |

## 5. 业务外围服务映射

这些服务会参与主部署，但并不直接对应本仓库的业务源码模块。

| Compose 服务名 | 镜像/构建来源 | 是否本仓库源码构建 | 作用 | 与业务模块关系 |
| --- | --- | --- | --- | --- |
| `nginx` | `nginx:latest` | 否 | 反向代理，统一对外入口 | 代理到 `api` 与 `web` |
| `db` | `postgres:15-alpine` | 否 | 主业务数据库 | `api`/`worker`/`plugin_daemon` 依赖 |
| `redis` | `redis:6-alpine` | 否 | 缓存与 Celery Broker | `api`/`worker`/`worker_beat`/`plugin_daemon` 依赖 |
| `sandbox` | `langgenius/dify-sandbox:0.2.12` | 否 | 代码执行沙箱 | `api` 工作流代码执行能力依赖 |
| `plugin_daemon` | `langgenius/dify-plugin-daemon:0.3.0b1-local` | 否 | 插件运行与安装守护进程 | 与 `api` 通过内部 API 协作 |
| `ssrf_proxy` | `ubuntu/squid:latest` | 否 | SSRF 隔离代理 | `sandbox` 和部分网络访问链路依赖 |
| `certbot` | `certbot/certbot` | 否 | HTTPS 证书申请/续签 | 与 `nginx` 协作，可选 profile |

### 5.1 `nginx` 的特殊性

`nginx` 虽然不是本仓库源码镜像，但 `docker/nginx/` 目录实际承载了它的“本地配置模块”：

- `docker/nginx/nginx.conf.template`
- `docker/nginx/proxy.conf.template`
- `docker/nginx/https.conf.template`
- `docker/nginx/docker-entrypoint.sh`

因此可以把它理解为：

- 仓库中的“配置模块” -> 官方 `nginx` 镜像 -> `nginx` 容器

### 5.2 `ssrf_proxy` 的特殊性

`ssrf_proxy` 同理，代码并不在仓库里，但本仓库提供了配置模板：

- `docker/ssrf_proxy/squid.conf.template`
- `docker/ssrf_proxy/docker-entrypoint.sh`

映射关系为：

- 仓库中的 SSRF 配置模块 -> `ubuntu/squid` 镜像 -> `ssrf_proxy` 容器

## 6. 可选向量库与扩展基础设施

`docker/docker-compose.yaml` 中包含大量通过 `profiles` 控制启停的可选服务。它们的共同特点是：

- 一般不对应本仓库业务源码
- 主要作为 `api/worker` 的外部依赖
- 通过环境变量决定是否启用，以及由哪种 `VECTOR_STORE` / `ETL_TYPE` 使用

### 6.1 向量库服务总览

| Compose 服务名 | 镜像/构建来源 | profile | 说明 |
| --- | --- | --- | --- |
| `weaviate` | `semitechnologies/weaviate:1.19.0` | `weaviate` / 默认 | 默认向量库之一 |
| `qdrant` | `langgenius/qdrant:v1.7.3` | `qdrant` | Qdrant 向量库 |
| `couchbase-server` | `build: ./couchbase-server` | `couchbase` | 当前仓库内唯一额外自定义构建的向量库镜像 |
| `pgvector` | `pgvector/pgvector:pg16` | `pgvector` | Postgres + pgvector |
| `vastbase` | `vastdata/vastbase-vector` | `vastbase` | Vastbase |
| `pgvecto-rs` | `tensorchord/pgvecto-rs:pg16-v0.3.0` | `pgvecto-rs` | pgvecto-rs |
| `chroma` | `ghcr.io/chroma-core/chroma:0.5.20` | `chroma` | Chroma |
| `oceanbase` | `oceanbase/oceanbase-ce:4.3.5-lts` | `oceanbase` | OceanBase 向量库方案 |
| `oracle` | `container-registry.oracle.com/database/free:latest` | `oracle` | Oracle 向量数据库方案 |
| `opensearch` | `opensearchproject/opensearch:latest` | `opensearch` | OpenSearch |
| `opensearch-dashboards` | `opensearchproject/opensearch-dashboards:latest` | `opensearch` | OpenSearch 可视化 |
| `opengauss` | `opengauss/opengauss:7.0.0-RC1` | `opengauss` | openGauss |
| `myscale` | `myscale/myscaledb:1.6.4` | `myscale` | MyScale |
| `matrixone` | `matrixorigin/matrixone:2.1.1` | `matrixone` | MatrixOne |
| `elasticsearch` | `docker.elastic.co/elasticsearch/elasticsearch:8.14.3` | `elasticsearch` | Elasticsearch |
| `kibana` | `docker.elastic.co/kibana/kibana:8.14.3` | `elasticsearch` | Kibana |

### 6.2 Milvus 是一组容器，不是单个容器

Milvus 方案不是一个镜像对应一个容器，而是一套组合：

| Compose 服务名 | 镜像 | 作用 |
| --- | --- | --- |
| `etcd` | `quay.io/coreos/etcd:v3.5.5` | Milvus 元数据存储 |
| `minio` | `minio/minio:RELEASE.2023-03-20T20-16-18Z` | Milvus 对象存储 |
| `milvus-standalone` | `milvusdb/milvus:v2.5.15` | Milvus 主服务 |

因此从业务角度应理解为：

- `api/worker` -> Milvus 依赖集群
- 而不是简单的 `api/worker` -> 单个 `milvus` 容器

### 6.3 ETL 扩展服务

| Compose 服务名 | 镜像 | profile | 说明 |
| --- | --- | --- | --- |
| `unstructured` | `downloads.unstructured.io/unstructured-io/unstructured-api:latest` | `unstructured` | 文档解析 ETL 服务 |

当 `ETL_TYPE=Unstructured` 时，`api/worker` 会依赖这个额外服务。

## 7. `docker-compose.middleware.yaml` 的角色

`docker/docker-compose.middleware.yaml` 不是完整部署，而是开发态或拆分部署场景下的“中间件集合编排”。

它包含的重点服务有：

- `db`
- `redis`
- `sandbox`
- `plugin_daemon`
- `ssrf_proxy`
- `weaviate`

这意味着在本地开发时，通常可以把：

- 业务代码运行在宿主机
- 中间件运行在 Docker

对应关系可以理解为：

| 运行对象 | 运行位置 |
| --- | --- |
| `api/` 代码 | 宿主机 Python 环境，或独立容器 |
| `web/` 代码 | 宿主机 Node 环境，或独立容器 |
| `db` / `redis` / `sandbox` / `plugin_daemon` / `weaviate` | Docker 容器 |

## 8. `docker/tidb/docker-compose.yaml` 的角色

`docker/tidb/docker-compose.yaml` 是独立于主编排的 TiDB 部署示例，不直接并入主 `docker-compose.yaml`。

它包含以下容器：

| Compose 服务名 | 镜像 | 作用 |
| --- | --- | --- |
| `pd0` | `pingcap/pd:v8.5.1` | Placement Driver |
| `tikv` | `pingcap/tikv:v8.5.1` | 分布式 KV 存储 |
| `tidb` | `pingcap/tidb:v8.5.1` | SQL 层 |
| `tiflash` | `pingcap/tiflash:v8.5.1` | 列存分析节点 |

因此 TiDB 相关映射是：

- 本仓库无 TiDB 核心源码
- 仓库只提供独立编排配置
- 业务模块通过环境变量指向这组外部容器

## 9. 全局映射总表

### 9.1 核心业务与主服务

| 仓库模块/目录 | 镜像 | Compose 服务名 | 容器职责 |
| --- | --- | --- | --- |
| `api/` | `langgenius/dify-api:2.0.0-beta.2` | `api` | 后端 API |
| `api/` | `langgenius/dify-api:2.0.0-beta.2` | `worker` | Celery 异步任务 |
| `api/` | `langgenius/dify-api:2.0.0-beta.2` | `worker_beat` | Celery 定时任务 |
| `web/` | `langgenius/dify-web:2.0.0-beta.2` | `web` | 前端 Web 服务 |
| `docker/couchbase-server/` | 本地构建 | `couchbase-server` | Couchbase 向量库 |

### 9.2 仓库配置模块与官方镜像

| 仓库目录 | 镜像 | Compose 服务名 | 说明 |
| --- | --- | --- | --- |
| `docker/nginx/` | `nginx:latest` | `nginx` | 仓库提供配置模板和入口脚本 |
| `docker/ssrf_proxy/` | `ubuntu/squid:latest` | `ssrf_proxy` | 仓库提供 Squid 模板和入口脚本 |
| `docker/pgvector/` | `pgvector/pgvector:pg16` | `pgvector` | 仓库提供入口脚本 |
| `docker/elasticsearch/` | `docker.elastic.co/elasticsearch/elasticsearch:8.14.3` | `elasticsearch` | 仓库提供入口脚本 |

### 9.3 纯外部镜像服务

| Compose 服务名 | 镜像 | 说明 |
| --- | --- | --- |
| `db` | `postgres:15-alpine` | 主数据库 |
| `redis` | `redis:6-alpine` | 缓存与消息队列 |
| `sandbox` | `langgenius/dify-sandbox:0.2.12` | 代码执行沙箱 |
| `plugin_daemon` | `langgenius/dify-plugin-daemon:0.3.0b1-local` | 插件守护进程 |
| `certbot` | `certbot/certbot` | 证书管理 |
| `weaviate` 等各类向量库 | 外部镜像 | 作为 `api/worker` 的可选依赖 |

## 10. 推荐的理解方式

如果从架构视角看，当前工程最适合按下面四层理解：

### 10.1 业务代码层

- `api/`
- `web/`

### 10.2 容器化封装层

- `api/Dockerfile`
- `web/Dockerfile`
- `docker/couchbase-server/Dockerfile`

### 10.3 部署编排层

- `docker/docker-compose.yaml`
- `docker/docker-compose.middleware.yaml`
- `docker/tidb/docker-compose.yaml`

### 10.4 外部基础设施层

- PostgreSQL
- Redis
- Sandbox
- Plugin Daemon
- Nginx
- 各类 Vector Store
- Elasticsearch / OpenSearch / Milvus / TiDB 等

## 11. 一句话总结

当前仓库真正的核心业务模块只有 `api` 和 `web` 两大源码模块，其中 `api` 镜像被复用为 `api`、`worker`、`worker_beat` 三类容器；其余大部分容器属于数据库、缓存、网关、沙箱、插件和向量库等外围基础设施，仓库主要负责它们的 Compose 编排、配置模板和少量入口脚本定制。
