#!/bin/bash

set -euo pipefail

SCRIPT_DIR="$(dirname "$(realpath "$0")")"

echo "Step 1/2: start middleware containers"
"$SCRIPT_DIR/start-middleware"

echo
echo "Step 2/2: start application containers"
"$SCRIPT_DIR/start-docker-app"

